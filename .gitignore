/node_modules
/.pnp
.pnp.*
.yarn/*

/coverage

/.next/
/out/
/build

.DS_Store
*.pem

npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

.env*
!.env.example

# Local SQLite database and its WAL sidecars. Patient data — must never be
# committed, and on a VPS this directory is the thing to back up.
/data
*.db
*.db-shm
*.db-wal

.vercel

*.tsbuildinfo
next-env.d.ts
